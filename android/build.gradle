buildscript {
    ext.kotlin_version = '1.3.50'
    repositories {
        google()
        jcenter()
    }
    maven {
        url "https://plugins.gradle.org/m2/"
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.1.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "gradle.plugin.com.browserstack.gradle:browserstack-gradle-plugin:3.0.2"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

subprojects {
    project.evaluationDependsOn(':app')
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

apply plugin: "com.browserstack.gradle"

browserStackConfig {
    username = "automaovesti1"
    accessKey = "HYamkp5sBqpXNh1zFz9s"
    configFilePath = "$buildDir\bs.conf.js"
}
